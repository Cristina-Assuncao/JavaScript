// Actvity 1
/*
const sayHello = () => {
    console.log("Hi there, how are you?")
}
sayHello()

const hollidayDest = (country, month) => {    // the arrow function takes parameters inside ()
    console.log(`Yay! I am going to ${country}, in ${month}`)
}
hollidayDest('France', 'April')               // call the funcion and inside () are the arguments
*/

// Activity 2
const sayHello = (myName, drink) => {
    console.log(`Hi, my name is ${myName}, would you like an ${drink}?`)
}
sayHello("Cristina", "orange juice")

const multiply = (num1, num2) => {
    return num1 * num2
}
console.log(multiply(2, 5))
